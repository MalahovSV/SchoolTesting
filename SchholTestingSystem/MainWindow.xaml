<Window x:Class="SchholTestingSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchholTestingSystem"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="389*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="37*"/>
            <ColumnDefinition Width="163*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Name="ButtonsPanel">
            <Button Name="NewsButton" Click="NewsButton_Click">News</Button>
            <Button Name="HomeButton" Click="HomeButton_Click">Home</Button>
            <Button Name="TestsButton" Click="TestsButton_Click">Tests</Button>
            <Button Name="SettingsButton" Click="SettingsButton_Click">Settigs</Button>
            <Button Name="HelpButton" Click="HelpButton_Click">Help</Button>
        </StackPanel>
        
        <Grid Grid.Column="1" Grid.Row="1" Name="ViewPanel">
            
        </Grid>

    </Grid>
</Window>
